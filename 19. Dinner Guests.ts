import { guest_list } from '../14. Guest List/app.guestlist';
console.log (`\nPrinting Number of guest List`);
console.log (`We Had finally invited 4 people for the party out of ${guest_list.length}`);